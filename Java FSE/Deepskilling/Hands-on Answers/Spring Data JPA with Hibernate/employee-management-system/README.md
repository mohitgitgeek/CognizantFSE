# Spring Data JPA and Hibernate solutions

This project implements the employee-management exercises: H2 configuration, entity mapping and relationships, repositories, derived and JPQL query methods, CRUD REST APIs, paging/sorting, auditing, projections, and Hibernate JDBC batch settings. The `EmployeeRepository` demonstrates fetch joins, aggregates, and bulk updates; Spring Data provides the required JPA abstraction.

# Spring REST hands-on solution

`/hello`, country GET/POST/PUT/DELETE endpoints, employee list/CRUD endpoints, validation, global JSON error responses, logging configuration, and `/departments` are implemented. JWT can be layered in using Spring Security's resource-server support; the original worksheet's `WebSecurityConfigurerAdapter` is obsolete in modern Spring Security.
